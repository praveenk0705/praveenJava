
public class Insert {

    private Comparable findMin() {
        throw new UnsupportedOperationException("Not supported yet."); 
    }

    private void Str_bott(int i) {
        throw new UnsupportedOperationException("Not supported yet."); 
    }

    private boolean isEmpty() {
        throw new UnsupportedOperationException("Not supported yet."); 
    }
public class Position {






public Comparable findMin()
{
    if (isEmpty())
try {
throw new Exception("Srorage space is empty");
} catch (Exception e) {
}
    Comparable[] array = null;
return array[1];
}
}
    


public Comparable deleteMin()
{
        int currentSize = items.length;
        Comparable[] array = new Comparable[items.length + 1];
Comparable minItem = findMin();
        int value = 0;
array[1] = array[value--];
Str_bott(1);
return minItem;
}

   






}
